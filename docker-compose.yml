networks:
  proxy:
    external: true
    name: traefik-proxy

x-ypm-service: &ypm-service
  build: .
  init: true
  healthcheck:
    test: ["CMD-SHELL", "test ! -f /tmp/drain && curl -sf http://localhost:8001/health || exit 1"]
    interval: 30s
    timeout: 10s
    retries: 3
    start_period: 60s
  networks:
    - proxy
  restart: unless-stopped
  stop_grace_period: 30s

services:
  ypm-ethereum:
    <<: *ypm-service
    environment:
      CHAIN_NAME: ethereum
      CHAIN_ID: 1
      RPC_URL: ${RPC_URL_ETHEREUM}
      ETHERSCAN_TOKEN: ${ETHERSCAN_TOKEN}
    volumes:
      - cache-ethereum:/data/cache
      - brownie-ethereum:/root/.brownie
    labels:
      traefik.enable: "true"
      docker-rollout.pre-stop-hook: "touch /tmp/drain && sleep 10"
      traefik.http.routers.ethereum.rule: "Host(`${YPM_HOST:-localhost}`) && PathPrefix(`/ethereum`)"
      traefik.http.routers.ethereum.entrypoints: web
      traefik.http.middlewares.strip-ethereum.stripprefix.prefixes: /ethereum
      traefik.http.routers.ethereum.middlewares: strip-ethereum
      traefik.http.services.ethereum.loadbalancer.server.port: "8001"
      traefik.http.routers.health.rule: "Host(`${YPM_HOST:-localhost}`) && Path(`/health`)"
      traefik.http.routers.health.entrypoints: web
      traefik.http.routers.health.service: ethereum


  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    labels:
      traefik.enable: "true"
      docker-rollout.pre-stop-hook: "touch /tmp/drain && sleep 10"
      traefik.http.routers.frontend.rule: "Host(`${YPM_HOST:-localhost}`) && PathPrefix(`/`)"
      traefik.http.routers.frontend.entrypoints: web
      traefik.http.routers.frontend.priority: "1"
      traefik.http.services.frontend.loadbalancer.server.port: "8080"
    networks:
      - proxy
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:8080/"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    restart: unless-stopped
    stop_grace_period: 30s

volumes:
  cache-ethereum:
  brownie-ethereum:
